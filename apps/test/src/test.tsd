import {View} from "@dlightjs/dlight";
import {_, button, div, State} from "@dlightjs/useless";


class Tjj extends View {
    Body() {
        div("hfh")
    }
}

let a = {
    ok: Tjj
}
console.log((a["oo"] ?? a.ok))
export class TestTest extends View {
    @State text = ""
    @State apples = [1,2,3]
    @State toggle = false
    asts = []
    aa = Tjj

    Body() {
        (a["oo"])("ok")
    }
}