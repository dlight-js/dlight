import {View} from "@dlightjs/dlight";
import {_, button, div, Prop, State} from "@dlightjs/useless";


class TT extends View {
    @Prop hh = 0

    Body() {
        div(`iam TT ${this.hh}`)
        // _(this._$children)
    }
}

export class TestTest extends View {
    @State text = ""
    @State apples = [1,2,3]
    @State orange = [1,2,3]
    @State toggle = true
    @State hh = 1
    @State fine = 2
    asts = []


    Body() {
        div("hh")
        button("a")
            .onclick(() => {
                this.toggle = !this.toggle
                this.hh += 1
            })
        TT()
            .hh(this.hh)
        {
            if (this.toggle) {
                for (let i of [...Array(100000).keys()]) {
                    div(i)
                }
            }
        }
    }
}
