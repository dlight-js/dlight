import {View} from "@dlightjs/dlight";

export class SubComp extends View {
    @Env okk
    Body() {
        div(this.okk)
    }
}

export class TestConcurr extends View {

    Body() {
        Environment({okk: "what"}) {
            {{ {{SubComp()}} }}
        }
    }
}
