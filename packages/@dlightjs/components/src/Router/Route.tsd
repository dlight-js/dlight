import {Exp, View} from "@dlightjs/dlight";
import {_, Prop, State} from "@dlightjs/useless";

export class Route extends View {
    @Prop _$content = " none" // 空格不是合法url，所以不会有问题
    Body() {
        _(this._$children)
    }
}
