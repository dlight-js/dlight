import {View} from "./core";



class Element extends View {
    @Prop count=0
    @PropState okk = 1

    Body() {
        div("-----")
        div(this.count)
        div(this.okk)
        button()
            .onclick(() => {
                this.okk--
            })
    }
}
export class TestTest extends View {
    @Prop jjj = 1
    @State toggle1 = this.jjj + 1
    @Prop hht = this.jjj + 100
    // toggle4 = this.jjj + this.toggle1 + 1
    // toggle2 = this.toggle4 + this.jjj
    // toggle3 = {ok: () => this.toggle1 + 1}

    Body() {
        button("+")
            .onclick(() => {
                this.toggle1 ++
                // this.jjj ++
            })
        div (this.hht)
        div (this.toggle1)
        // div (this.toggle2)
        // Element({count: this.toggle1 + 1})
        Element({okk: this.toggle1})
        // Element({count: this.toggle1})
    }
}


