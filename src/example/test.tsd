import {View} from "@/core";
import {HStack, Spacer, Node, Button, TextField} from "@/components"


export class SubElement extends View {
    @State fruitNames
    @State fruitType = 0
    fruitNames = ['banana', 'orange', 'grape']

    Body() {
        TextField('placeholder')
            .placeholder('hahah')
            

    }
}
